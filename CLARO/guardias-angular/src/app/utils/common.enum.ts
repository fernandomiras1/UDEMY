export enum GroupType {
  SITIO = 'sitio',
  TECNOLOGIA = 'tecnologia',
}

export enum MODE_STATUS_TEMPLATE {
  LIST = 'list', // lista plantillas
  SELECTION = 'selection', // seleccion al crear grupos
  EDITION = 'edition' // agregar y quitar plantillas al editar grupos
}

export enum ROLE {
  ADMIN = 'ARG-MV-GESTION Y SOPORTE OPERATIVO'
}

export enum USER_PROFILE {
  ADMIN = 'admin'
}

export enum ToggleType {
  OPEN = -1,
  CLOSED = -2
}

export enum MODE_SELECT_TIME {
  DAYS = 'fourDays',
  WEEKS = 'twoWeeks'
}
